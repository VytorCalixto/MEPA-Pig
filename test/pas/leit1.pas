(* Imprime uma variavel simples *)
program p (input, output);
var x, y, z, o , p : integer;
    coisa : integer;
begin
    read(x);
    x := 100;
    y := y;
    y := x;
    coisa := 5;
    z := p;
    write(x)
end.
